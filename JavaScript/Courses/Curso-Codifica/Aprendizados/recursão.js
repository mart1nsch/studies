const prompt = require('prompt-sync')();

// Faz uma soma recursiva de 1 até o número digitado
/*const numero = Number(prompt("Digite um número para somar de 1 até o número: "));

function soma(n) {
    if (n === 1) {
        return n;
    }
    return n + soma(n-1);
}

console.log(soma(numero));*/

// Faz o fibonacci recursivo
/*const numeroVezesFibonacci = Number(prompt("Quantos números da sequência fibonacci quer ver: "));



function fibonacci(n) {
    if (n === 0 || n === 1) {
        return n;
    }

    return fibonacci(n-1) + fibonacci(n-2);
}

console.log(fibonacci(numeroVezesFibonacci));*/